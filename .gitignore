# Secrets & env
.env
.env.local
*.local.json

# User-specific config, profile, and local data
config.yaml
resume.txt
data/
service-account.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# n8n + postgres runtime data (self-hosted)
n8n_data/
postgres_data/
.n8n/

# Node / build
node_modules/
dist/
build/

# OS / misc
.DS_Store
Thumbs.db
nul
*.log

# Applier profile (facts used to answer employer questions)
profile.yaml
# Hand-written evidence: which repo is which product, and what must never be said
career.yaml

# Reports the export and dry-run tools write. Generated on demand from the
# database, and they contain the actual job listings a run collected.
jobs-*.csv
unpriced-report.*

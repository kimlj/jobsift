httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
PyYAML>=6.0
python-dotenv>=1.0

# LLM providers — install whichever one your config.yaml `llm_provider` uses
# (both are listed so `pip install -r` just works; drop the one you don't need):
anthropic>=0.40
openai>=1.40

# Only needed if you enable the Google Sheet output:
gspread>=6.0
google-auth>=2.30
